# Heavy ML deps — install ON TOP OF requirements.txt ONLY to train the
# win-probability model (app/ai/train/train_win_probability.py):
#   pip install -r requirements.txt -r requirements-ml.txt
#
# NOT in the deployed image — the runtime uses the transparent heuristic
# predictors (no numpy/sklearn needed). LLM commentary uses Gemini over httpx
# (already a runtime dep), so no LLM package is required for serving either.
numpy==2.2.1
pandas==2.2.3
scikit-learn==1.6.1
xgboost==2.1.3
lightgbm==4.5.0
joblib==1.4.2
