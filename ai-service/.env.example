# AI service environment. Copy to `.env`.
DEBUG=true

# Optional — enables LLM commentary/summaries. Without it, templates are used.
OPENAI_API_KEY=
LLM_MODEL=gpt-4o-mini

# Where trained model artifacts live. Absent → heuristic predictors are used.
MODEL_DIR=models
