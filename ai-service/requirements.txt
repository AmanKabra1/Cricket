# Runtime deps (slim) — the heuristic predictors and template commentary use
# only the stdlib, so the deployed service stays small and builds on free tiers.
# For model training and LLM features, also install requirements-ml.txt.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
httpx==0.28.1

# Dev / test
pytest==8.3.4
