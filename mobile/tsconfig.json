{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // No baseUrl (deprecated in TS 7): paths resolve relative to this file.
    "paths": {
      "@/*": [
        "./src/*"
      ]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx"
  ]
}
